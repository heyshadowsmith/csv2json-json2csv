# csv2json-json2csv
csv2json-json2csv is a small project that can be used to take in JSON from a variety of sources and convert the data to a CSV. It can also take a CSV and convert it into a JSON object.
